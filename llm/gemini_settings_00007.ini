[GEMINI_SETTINGS]
# 사용할 모델명 (예: gemini-2.5-flash, gemini-3-flash-preview)
model = gemini-2.5-flash

# 분석할 파일 경로
file_path = scheduler_engine.txt

# Gemini에게 시킬 명령
prompt = 이 문서 핵심 내용을 10줄로 요약해줘.

# 기타 설정
chunk_chars = 12000

#결과 출력파일
output_file_path = llm_analysis.docx
